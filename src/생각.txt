기능을 어떻게 쓸지 고민이 많음
일단 기본crud 로 웹까지 만든 다음 기능 추가하는 편이 빠를것 같다는 생각


Transactional 은 트랜잭션이 끝날때까지 db에 반영이 안됨
따라서 save 를 먼저 쓰고 나중에 관계설정을 하더라도 관계설정이 다 된 상태로 db에 반영

ManyToMany 는 생각보다 까다롭다.
뭐 하나를 건드리려면 상대까지 같이 건드려야하니까

transactional 의 영속성 컨텍스트관리를 이용